# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# Secrets — NEVER commit
.env
.env.local
service-account.json
**/service-account*.json
**/firebase-adminsdk-*.json
# Firebase Web SDK config — apiKey/appId are public-by-design identifiers
# (security enforced via Firebase Rules), so these are tracked so EAS can
# build them. Real secrets (.env, service-account.json) remain ignored above.

# Uploads & generated
backend/uploads/
*.log

# Node / Expo
node_modules/
.expo/
.expo-shared/
dist/
web-build/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
